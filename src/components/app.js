// we have installed the react in node modules
// this gives file wide access to React
import React, { Component } from 'react';

export default class App extends Component {
  render() {
    return (
      <div>React-Redux baseline by Shiraz Bhalla</div>
    );
  }
}
